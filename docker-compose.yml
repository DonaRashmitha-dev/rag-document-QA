version: "3.9"

services:
  fault-injection:
    build: .
    ports:
      - "5000:5000"
    environment:
      - HOST=0.0.0.0
      - PORT=5000
      - API_TOKEN=${API_TOKEN:-changeme123}
    restart: unless-stopped
